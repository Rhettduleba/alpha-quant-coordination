# BASELINE_2026-06-18 — frozen pre-change strategy (Planning Loop 121 STEP 1)

Frozen reference of the EXACT live SIM strategy BEFORE the approved Loop-121 change
(PROP-EXIT-CANDLE-1.4ATR + PROP-REENTRY-CAP-1, both Rhett-approved 2026-06-18). This is
the "before" the forward shadow + kill-condition measure against.

**Watched-config combined hash (before):** `7e426dc69a492984c7eaa2a2b96271ad...` (9 watched files)
**Captured:** 2026-06-19 ~09:29 ET (config unchanged since 6/18).

## Entry
- 9:35 ORB on in-play-gated names: day-RelVol ≥ 1.5, move-band 2–9%, $20M/20d dollar-volume floor, MIN_PRICE $20.
- Rank top-N by RelVol; stop-LIMIT entry at OR high/low with 5 bps collar.
- Sized ~$20k vs $400k deploy base (`MAX_POSITION_NOTIONAL=25000`, `DEPLOY_BASE=400000`).
- Re-arm windows through the session (10:35 / 11:35 / 12:35 / 1:35 / 2:35).

## Exit — V0 (the thing being REPLACED)
- `ORB_EXIT_MODE = "candle_close"`.
- Phase-1 tight stop = **0.15×ATR** from fill (`PHASE1_ATR=0.15`; resting ORB_SL also 0.15 via `EXIT_SL_FRAC`).
- On confirm (price +0.15×ATR favorable) → candle-close trail → exit on first opposite candle close.
- Catastrophe backstop = **1.0×ATR** (`CATASTROPHE_ATR=1.0`).

## Re-entry — R0 (the thing being REPLACED)
- Re-entry through the same gate, **no per-name cap** (`ORB_MAX_ENTRIES_PER_NAME` absent = uncapped).

## Sizing / caps (unchanged by this change)
- `MAX_POSITION_NOTIONAL=25000` · `MAX_OPEN_POSITIONS=16` · `DEPLOY_BASE=400000` · `MAX_LEVERAGE=4.0`.
- `DAILY_MAX_LOSS=1e9` (SIM-disabled sentinel) · `ACCOUNT_DD_KILL_ENABLED=False`.

## V0/R0 broker-truth performance to date (verified `day_totals`)
| date | NET | n | win% | PF | note |
|---|---|---|---|---|---|
| 2026-06-16 | +$337.38 | 33 | 45.5% | 1.17 | confounded (intraday outage emptied the book) |
| 2026-06-17 | +$138.04 | 52 | 53.8% | 1.04 | |
| 2026-06-18 | +$1.99 | 41 | 53.7% | 1.00 | |

**3-day signature (the change rationale):**
- Every 0.15×ATR phase-1 stop-out was a LOSS; candle-close exits were near-all wins (perfect bifurcation 6/17).
- Payoff ratio inverted (avg loss > avg win) — tight stop kills trades before they confirm.
- 6/18 first-entries +$579 vs re-entries −$577 (re-entry drag).

## What replaces it (Loop 121, deploy AFTER CLOSE)
- Exit → `ORB_EXIT_MODE="candle_1.4atr_chandelier"`: candle-close trail + a single 1.4×ATR ratchet
  chandelier (replaces BOTH the 0.15 phase-1 and the 1.0 catastrophe); exit = earlier of opposite-candle-close
  OR 1.4×ATR chandelier; never unprotected.
- Re-entry → `ORB_MAX_ENTRIES_PER_NAME=1` (R1; reject arming a name already entered today, both directions).
- Both flag-gated + reversible to V0/R0; V0 + R0 kept in forward shadow for attribution; pre-registered kill
  condition (5 forward days, must BEAT V0-shadow net-of-cost).
