# FOUNDATION MAP -- verified-vs-assumed whole-system component audit (READ-ONLY)
_Tags: [V] verified from live code/broker-truth · [M] measured against OUTCOMES (cites report) · [A] assumed / not-yet-verified. No fixes proposed here -- this is the MAP. Freeze intact._

## LEDGER
| # | Component | (1) WHAT IT ACTUALLY DOES | (2) WHY BUILT | (3) MEASURED vs OUTCOMES? | (4) VERDICT |
|---|---|---|---|---|---|
| 1 | **UNIVERSE BUILD  (orb_universe.build_universe)** | [V] 530-name static list = S&P500 + curated supplement, leveraged-ETF filtered, 7-day cache (orb_universe.py:105-142). [V] HTB/halted exclusion IS wired + FAIL-SAFE: arms only names confirmed borrowable+non-halted, excludes NO_QUOTE (orb_runner.py:652-655; ORB_EXCLUDE_HARD_TO_BORROW=True, htb_filter.py). [V] NOTE: this is a DIFFERENT universe from the 145-name research_brain_v1 `advisor_universe_latest.json` -- ORB scans the 530 list, NOT the research-brain channel (symbol_universe uses that channel only for a sector map). | [A] Zarattini ORB needs a broad, liquid, shortable large-cap pool; S&P500 is the standard liquid set; HTB exclusion added 2026-06-15 (Rhett) because the strategy trades both sides and HTB names 400 on short locate. | NO -- the universe COMPOSITION's effect on outcomes has never been audited (does S&P500-only cost us the best movers? are small/mid movers excluded that would trade better?). | **UNPROVEN (mechanically sound + HTB-safe; outcome effect unmeasured).** |
| 2 | **SELECTION / IN-PLAY GATE** | [V] gate now ORB_INPLAY_GATE=False (DISABLED Loop 158, LIVE Mon 6/29). When ON it applied RelVol floor + move-band + $-vol floor + top-N + direction-match (inplay_gate.py); [M] it ANTI-SELECTED. Selection now reverts to old top-N-by-RelVol; the INDEX-ETF exclusion is PRESERVED standalone in orb_runner's gate-off branch; HTB/halted exclusion is separate + unaffected. | [A] Intended to keep only genuine in-play movers (high RelVol, real move) and cut chop -- a quality filter on the breakout candidate set. | YES -- gate_effect_analysis.py (2026-06-27). | **DISABLED (was BROKEN/anti-selects). [M] rejected-name counterfactual +$48/trade PF1.98 vs passed broker-truth -$29/trade PF0.66; 67% of winning re-arm names (60/89, +$8.1k) would be BLOCKED; 9:35 loss concentrates in mega-caps -$229/trade; gate forces DIRECTION-MATCH -> every gapped entry is with-gap (gap_fade_test). Turned OFF Loop 158 (Rhett+Planning); rebuild DEFERRED until a validated entry signature exists (building one unvalidated repeats the original mistake).** |
| 3 | **ENTRY TRIGGER  (Zarattini 5-min ORB)** | [V] opening range = first 5 min (09:30->09:35 ET); LONG on break of OR high, SHORT on break of OR low; scan/arm 09:35:30 (orb_runner.py:459-460,:4). [V] entry = stop-LIMIT with ~5bps marketable collar that CAPS slippage (orb_runner.py:774-792, ORB_MARKETABLE_LIMIT_COLLAR_BPS). Re-arm path = same trigger on 5-min ranges in 10:35-14:35 windows (orb_multiscan). | [A] Zarattini (2023) ORB is the researched H1 hypothesis; stop-limit collar chosen 2026-06 to cap the entry slippage a stop-market was eating. | PARTIAL -- PATH-level measured (gross_signal_quality: 9:35 path -$29/trade = loss engine, re-arm +$19.7/trade PF1.30 = winning), and execution is clean (execution_cost_ledger: entry 0.82 bps, delay 2.52 offset by exec -1.7). But the TRIGGER MECHANICS edge is NOT isolated from selection/gap/timing. | **MIXED: execution mechanics WORK (cheap, collar-capped); the 9:35 trigger PATH loses but is confounded with the broken gate + gap-direction; the re-arm trigger path is gross-positive. Trigger logic per se = UNPROVEN (not isolated).** |
| 4 | **EXIT / STOP COVERAGE  (candle_close + 1.4xATR chandelier + resting stop)** | [V] live mode candle_1.4atr_chandelier: resting broker StopMarket at 1.4xATR. **REBUILT Loop 155 (LIVE Mon 6/29):** re-arm fills now ALSO register into the shared monitor (re-arm resting-stop coverage 0%->~100%), and the Tape Watcher (tape_watcher --live-exit) is the tick-fast exit OWNER holding the exit_ownership lease while exit_bot_v2 defers (no-double-exit is STRUCTURAL: flatten no-ops on flat). Kill-switch = tw_abort.flag (Loop 157). Confirm 0.15xATR -> candle-close trail. EOD flatten 15:50 ET. | [A] Chandelier+candle-close = let winners run, ratchet a favorable-only floor; resting broker stop = a hard catastrophe backstop. Loop-123 widened the resting floor to 1.4xATR. | YES -- forward-tested to a KILL 6/25; stop_coverage_audit.py; K-sweep exonerated 1.4 (Loop 154). | **K EXONERATED + REBUILT (timing/coverage, not the multiple). [M] Loop-123 exit KILLED 6/25 (NET -$2,017; MU -$1,670); K-sweep (Loop 154) showed the ~$7k bleed is execution LAG + the re-arm 0% gap, NOT K=1.4 (flat plateau K>=1.0). FIXES LIVE Mon 6/29: re-arm coverage 0%->~100% + TW fast single-owner exit + kill-switch. PRE-rebuild coverage was 37.9% (9:35 96% / RE-ARM 0%), ~7.2min late. Monday EOD reconcile is the live proof.** |
| 5 | **SIZING** | [V] shares = MIN(risk_shares, lev_shares): risk_shares=(RISK_PER_TRADE x REAL_CAPITAL_BASE)/sl_per_share (RISK-BASED), lev_shares=per_position_notional/entry_ref where per_position_notional = TARGET_DAY_TRADE_GROSS/TOP_N (~$15k) (orb_runner.py:727-730). Caps: per-position $25,000, per-side 0.5 (~$200k), deploy base $400,000 x 0.95 = deploy target, hard gross cap $400,000. Sized off intended LIVE $100k->$400k base, NOT $993k SIM equity. | [A] Fixed near-equal notional per name to spread a day-trade book across top-N; risk_shares leg added so a wide-ATR name doesn't oversize. Real-capital base (not SIM equity) so SIM behaves like live. | NO -- sizing's effect on outcomes never isolated (does MIN(risk,notional) help/hurt vs pure risk-parity or vol-target? never checked). | **UNPROVEN (it is risk-aware, not naive fixed-$ as feared; but outcome effect unmeasured).** |
| 6 | **RISK / CORRELATION** | [V] controls present: MAX_OPEN_POSITIONS=16 (count backstop), per-position + per-side notional caps, sector cap MAX_SECTOR_POSITIONS=2, DAILY_MAX_LOSS=1,000,000,000 (INTENTIONALLY OFF in SIM = True; MUST set before live). [V] NO correlation / factor / beta / concentration control exists (grep clean). | [A] Sector cap + count/notional caps were the chosen diversification proxy; daily-loss off deliberately to gather full SIM sessions good AND bad. | NO -- correlation/factor concentration never measured (e.g. a mega-cap-tech-heavy day = correlated risk; MU+NVDA+AAPL all moved together 6/26). | **UNPROVEN + KNOWN GAP: no correlation control; DAILY_MAX_LOSS OFF is a hard before-live gate.** |
| 7 | **ADVISOR  (claude-sonnet-4-6, 3x/day)** | [V][M] writes active_controls the bot validates CONTROL_VALID, but it is INERT on the book: 100% of trades are ORB; ORB reads ONLY should_block_entry (hard blocks); the soft controls (WATCHLIST/PROMOTE/REQUIRE_MIN/SET_MAX_PCT) are read by the composite path that took 0 trades. Its only book-reaching control (BLOCK_SYMBOL) was 192/192 broker-[NO] relays on 3 symbol-days. Look-ahead: CLEAN in live op. | [A] 'agent knows a lot, bot does only what's proven' -- a one-way typed-control valve, fail-open by design so a stale advisor never locks the bot out. | YES -- advisor_audit.py (Loop 150-151, 2026-06-28). | **INERT. [M] changed 0 of 285 trades, $0 gross effect. (Still does ADVISORY human-facing work; secondary: per-symbol P&L still leaks into the prompt -- strategy-not-symbols partial regression.)** |
| 8 | **SAFETY / INFRA** | [V] loop-stall instrumented this session (run_bot.py per-stage timing + observe-only 90s watchdog; NO kill) after recurring ~9:30 freezes. [V] the software-poll exit (exit_bot_v2) is the PRIMARY protection for 62% of fills (re-arm 0% resting coverage) -> its death = naked positions. supervisor_guardian watches supervisors. [A] exit-monitor-death exposure not yet load-tested. | [A] Single-box VPS; supervisors + guardian added incrementally after each incident (6/01-6/02 lessons). | NO -- survivability not adversarially tested (preflight checks current state, not 'survives the events I'm about to allow'). | **UNPROVEN/FRAGILE: instrumented but the exit-process-death naked-position path is a real before-live safety gap (ties to component 4's 0% re-arm coverage).** |

## VERDICT TALLY
- **BROKEN / INERT (measured):** #2 gate (anti-selects), #4 exit (killed + 0% re-arm coverage), #7 advisor (inert).
- **UNPROVEN (mechanically OK, outcome UNMEASURED):** #1 universe, #3 entry-trigger logic, #5 sizing, #6 risk/correlation, #8 infra-survivability.

## RANKED REBUILD PRIORITY (falls out of the ledger -- do NOT act yet; gated one-at-a-time)
1. **#2 SELECTION / IN-PLAY GATE** -- a CORE selector that is measured to ACTIVELY anti-select (rejects winners, keeps fade-prone mega-caps). Highest leverage; recalibrate/scope, OOS-test first.
2. **#4 EXIT timing + #8 the re-arm 0%-coverage safety gap** -- measured kill + a real before-live naked-position exposure. The confirmation-keyed exit gauntlet is the gated post-test track.
3. **#6 RISK/CORRELATION (add a correlation/concentration control) + restore DAILY_MAX_LOSS** -- known gaps; hard before-live gates, not optional.
4. **#1 / #3 / #5 (universe / trigger-logic / sizing)** -- audit BEFORE touching; unproven but not shown broken. Measure before rebuilding.
5. **#7 ADVISOR** -- lowest urgency: inert means it isn't HURTING. Decide later whether to wire it in (only after it has a proven edge) or leave as advisory. Fix the per-symbol-P&L prompt leak.

## STILL-UNMEASURED -> the next audits to run (where the next surprise hides)
- **#1 Universe composition** -- does S&P500-only exclude better movers? counterfactual on a wider criteria-based universe vs the 530.
- **#3 Entry-trigger logic, ISOLATED** -- strip out selection/gap/timing; does the raw OR-breakout trigger have an edge by itself (e.g. on a fixed clean name set)?
- **#5 Sizing** -- does MIN(risk,notional) beat pure risk-parity / vol-target on the realized book?
- **#6 Correlation/concentration** -- quantify correlated-risk days (mega-cap-tech clustering) and whether a cap would have helped.

## PRESERVE LIST (what WORKS -- strip the broken layers, keep the working core; do NOT break these)
- **The RE-ARM path** [M]: gross-positive (+$19.7/trade PF1.30; 10:35 window +$41/trade PF1.61) and UNGATED by design -- the gate's anti-selection does NOT touch it. (memory: do not touch the re-arm path.)
- **The SHORT side** [M]: +$11.2/trade PF1.18 gross.
- **Entry EXECUTION mechanics** [M]: clean -- 0.82 bps entry slippage, stop-limit collar caps it; delay offset by exec. Keep the stop-limit-collar entry.
- **HTB/halted fail-safe exclusion** [V] and the SIM-only guards -- safety, keep.
- **The advisor's one-way typed-control valve + fail-open** [V] -- the architecture is sound even though the content is inert; preserve the valve, fix the content/wiring later.

## READ-ONLY CONFIRMATION
- No orders, no watched-file edits, no behavior change, no advisor_control write. Freeze intact. Every claim tagged [V]/[M]/[A]. No fix proposed -- fixes are separate gated handoffs with pre-stated expected behavior -> shadow validation -> regression check.